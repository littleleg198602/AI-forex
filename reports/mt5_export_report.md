# MT5 Export Report

Data-only historical export diagnostics. No trading actions are performed.

No export attempts recorded. Run `python scripts/export_mt5_history.py ... --validate` locally to populate this report.
